%= include 'default_top'
%= include 'default_navigation'
<div id="content-wide">
% if( defined stash('message') ) {
<div class="message"><%= stash('message') %></div>
% }
<%= content %>
</div>
%= include 'default_bottom'
